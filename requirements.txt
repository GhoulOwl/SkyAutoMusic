pyautogui==0.9.54
keyboard==0.13.5
pywin32==306
psutil==5.9.8
# ---- 虚拟HID/驱动级键盘（Interception）----
# 提示：使用驱动级键盘需额外安装内核驱动 interception-driver（见 README）。
# 安装驱动后，按键注入在驱动层完成，可绕过部分游戏对 SendInput 的屏蔽。
# 未安装驱动时程序会自动回退到 keyboard / pyautogui，无需卸载本依赖。
# 注意：interception-python 需要 Python>=3.10。
interception-python>=1.13
# ---- 扒谱（音频转乐谱）功能依赖 ----
# 提示：扒谱使用 MuScriptor；Windows + NVIDIA 推荐用
# uv pip install --torch-backend=cu128 -r requirements.txt
# 首次运行需接受 Hugging Face 模型条款并登录：uvx hf auth login
soundfile>=0.14
muscriptor==0.2.1
# ---- 五维优化依赖（预处理 / 并行F0 / 起始点检测，均为可选增强）----
# 缺失时管道会自动降级：confidence 退回模型原始值，主流程不受影响。
librosa>=0.10
scipy>=1.10
numpy>=1.24
# crepe（CNN 基频）与 demucs（人声/乐器分离）按需安装：
#   pip install crepe        # 启用 f0_method=crepe 时需要
#   pip install demucs       # 启用 preprocess_separate=demucs 时需要
#（pyin 后端无需额外安装，仅需 librosa）
